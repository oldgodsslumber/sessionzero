{
  "name": "rpgshell",
  "version": "1.0.0",
  "private": true,
  "description": "Generic tabletop RPG shell \u2014 a static single-page app whose ruleset lives in a swappable system pack under systems/. This manifest exists only for the test suites; the app itself has no build step and no runtime dependencies.",
  "scripts": {
    "test": "node test/run.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
