{
  "name": "sessionzero",
  "version": "1.0.0",
  "private": true,
  "description": "Session Zero \u2014 a generic tabletop RPG shell. One engine, one folder per game; the ruleset lives in a swappable system pack under systems/. This manifest exists only for the test suites; the app has no build step and no runtime dependencies.",
  "scripts": {
    "test": "node test/run.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
